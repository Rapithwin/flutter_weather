import 'package:flutter/material.dart';

class ForecastEmpty extends StatelessWidget {
  const ForecastEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
